{
  "name": "auction-front",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w packages/auction-api-client && npm run build -w apps/web",
    "build:client": "npm run build -w packages/auction-api-client",
    "preview": "npm run preview -w apps/web"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
